<?xml version="1.0"?>
<odoo>
    <record id="res_partner_company_by_default_form" model="ir.ui.view">
        <field name="name">res.partner.company.by.default.form</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">

            <!-- When creating a contact from the main partner form, create individuals instead of companies -->
            <field name="child_ids" position="attributes">
                <attribute name="context">{'default_parent_id': active_id, 'default_street': street, 'default_street2': street2, 'default_city': city, 'default_state_id': state_id, 'default_zip': zip, 'default_country_id': country_id, 'default_supplier': supplier, 'default_customer': customer, 'default_lang': lang, 'default_is_company': False }</attribute>
            </field>
            <xpath expr="//field[@name='child_ids']/form//field[@name='type']" position="before">
                <field name="is_company" invisible="1"/>
            </xpath>

        </field>
    </record>
</odoo>