<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <!-- Inherit Form View to Modify it -->
    <record id="res_partner_view_form" model="ir.ui.view">
        <field name="name">res.partner.form</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form" />
        <field name="arch" type="xml">
            <button name="action_view_partner_invoices" position="after">
                <field name="has_subscriptions" invisible="1" />
                <button
                    name="%(action_pdf_cert_report)d"
                    string="Print PDF"
                    type="action"
                    icon="oi-odoo"
                    class="btn-primary"
                    invisible="not has_subscriptions"
                >
                    <div class="o_form_field o_stat_info">
                        <span class="o_stat_text">Print member certificate</span>
                    </div>
                </button>
            </button>
        </field>
    </record>
</odoo>
